# Optional. Without this key, the app uses its deterministic Mozaik demo runner.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini

# Optional Mozaik Cloud telemetry. The local runtime works without it.
MOZAIK_API_KEY=
