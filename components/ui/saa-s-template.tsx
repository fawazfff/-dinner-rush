"use client";

import Link from "next/link";
import { ArrowRight, Check, ChefHat, Flame, Menu, ShieldCheck, Soup, X, Zap } from "lucide-react";
import { AnimatePresence, motion } from "motion/react";
import { useState } from "react";

import { Button } from "@/components/ui/button";

function Navigation() {
  const [open, setOpen] = useState(false);
  return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/[0.07] bg-background/90 backdrop-blur-xl"><nav className="mx-auto flex h-16 max-w-[1440px] items-center justify-between px-5 sm:px-8"><Link href="/" className="flex items-center gap-2.5 text-sm font-bold tracking-[0.12em]"><span className="grid size-7 place-items-center rounded-[8px] bg-primary text-primary-foreground"><Zap className="size-4" fill="currentColor" /></span>DINNER RUSH</Link><div className="hidden items-center gap-7 md:flex"><a href="#how" className="text-xs text-muted-foreground hover:text-foreground">How it works</a><a href="#result" className="text-xs text-muted-foreground hover:text-foreground">What you get</a><Button asChild size="sm"><Link href="/kitchen">Play scenario <ArrowRight /></Link></Button></div><button type="button" className="grid size-10 place-items-center rounded-[10px] border border-border md:hidden" onClick={() => setOpen((value) => !value)} aria-label="Toggle menu">{open ? <X /> : <Menu />}</button></nav><AnimatePresence>{open ? <motion.div initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} className="border-t border-border bg-background p-5 md:hidden"><Button asChild className="w-full"><Link href="/kitchen">Play scenario</Link></Button></motion.div> : null}</AnimatePresence></header>;
}

function Preview() {
  const workers = [["Prep", ChefHat, "Rerouting side dish"], ["Grill", Flame, "Cooking burgers"], ["Fryer", Soup, "Fryer offline"], ["Expo", ShieldCheck, "Holding for safety"]] as const;
  return <div className="mx-auto w-full max-w-[650px] overflow-hidden rounded-[20px] border border-white/10 bg-[#12100e] shadow-2xl shadow-black/40"><div className="flex items-center justify-between border-b border-border px-4 py-3"><span className="text-[10px] text-muted-foreground">LIVE DINNER SERVICE</span><span className="flex items-center gap-2 text-[10px] text-primary"><span className="size-1.5 animate-pulse rounded-full bg-primary" /> 4 WORKING NOW</span></div><div className="p-4"><div className="mb-3 rounded-[12px] border border-destructive/35 bg-destructive/[0.05] p-3"><p className="text-xs font-semibold">The fryer just broke.</p><p className="mt-1 text-[10px] text-muted-foreground">Watch the kitchen adapt while your order is already cooking.</p></div><div className="grid grid-cols-2 gap-2">{workers.map(([name, Icon, task], index) => <motion.div key={name} initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .1 }} className="rounded-[12px] border border-border bg-white/[0.02] p-3"><div className="flex items-center justify-between"><Icon className="size-4 text-primary" /><span className="size-1.5 animate-pulse rounded-full bg-primary" /></div><p className="mt-5 text-xs font-semibold">{name}</p><p className="mt-1 text-[10px] text-muted-foreground">{task}</p></motion.div>)}</div><div className="mt-3 flex items-center gap-2 rounded-[12px] border border-primary/25 bg-primary/[0.05] p-3"><Check className="size-4 text-primary" /><div><p className="text-xs font-semibold">Kitchen recovered</p><p className="text-[10px] text-muted-foreground">Fries rerouted. Unsafe food stayed blocked.</p></div></div></div></div>;
}

export default function DinnerRushLanding() {
  return <main className="min-h-screen overflow-hidden bg-background text-foreground"><Navigation /><section className="relative mx-auto grid min-h-[760px] max-w-[1440px] items-center gap-12 px-5 pb-16 pt-28 sm:px-8 lg:grid-cols-[.85fr_1.15fr]"><motion.div initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }}><p className="mb-5 text-xs font-medium text-primary">YOU RUN THE RESTAURANT. AI RUNS THE KITCHEN.</p><h1 className="max-w-[680px] text-[clamp(3rem,6vw,6.3rem)] font-semibold leading-[.92] tracking-[-.065em]">Can your kitchen survive <span className="text-primary">dinner rush?</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-muted-foreground sm:text-lg">Build a menu, start service, then watch five AI kitchen workers cook together and react when things go wrong.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild size="lg"><Link href="/kitchen">Build your menu <ArrowRight /></Link></Button><a href="#how" className="px-3 py-2 text-sm text-muted-foreground hover:text-foreground">See how it works</a></div><p className="mt-5 text-xs text-muted-foreground">No setup. Pick food and press start.</p></motion.div><motion.div initial={{ opacity: 0, x: 18 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: .1 }}><Preview /></motion.div></section><section id="how" className="border-y border-border bg-[#100e0c]"><div className="mx-auto max-w-[1200px] px-5 py-20 sm:px-8"><div className="max-w-xl"><p className="text-xs text-primary">HOW IT WORKS</p><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">You create the problem. The kitchen figures it out.</h2></div><div className="mt-10 grid gap-px overflow-hidden rounded-[16px] border border-border bg-border md:grid-cols-3">{[["01", "Pick your menu", "Choose burgers, fries, salad, or mix them together."], ["02", "Choose the chaos", "Run a normal service or throw in a fryer failure and a late allergy."], ["03", "Watch the result", "See who did what, whether dinner was saved, and export the full kitchen report."]].map(([number, title, copy]) => <article key={number} className="bg-card p-6"><span className="text-xs text-primary">{number}</span><h3 className="mt-8 font-semibold">{title}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{copy}</p></article>)}</div></div></section><section id="result" className="mx-auto grid max-w-[1200px] gap-8 px-5 py-20 sm:px-8 lg:grid-cols-2 lg:items-center"><div><p className="text-xs text-primary">AFTER SERVICE</p><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Don't just watch AI move. See what it accomplished.</h2><p className="mt-4 max-w-lg text-sm leading-6 text-muted-foreground">Dinner Rush gives you a simple result, a plain-English decision history, and an exportable report. Judges can open Technical Proof to inspect the underlying Mozaik concurrency.</p></div><div className="rounded-[18px] border border-primary/25 bg-primary/[0.05] p-6"><p className="text-xs text-primary">SERVICE COMPLETE</p><p className="mt-2 text-xl font-semibold">Your kitchen handled the rush.</p><div className="mt-5 grid grid-cols-2 gap-2">{[["Orders served", "1"], ["Unsafe meals", "0"], ["Crisis recovered", "Yes"], ["AI workers together", "5×"]].map(([label, value]) => <div key={label} className="rounded-[10px] border border-border p-3"><p className="text-lg font-semibold">{value}</p><p className="text-[10px] text-muted-foreground">{label}</p></div>)}</div></div></section><section className="border-t border-border"><div className="mx-auto flex max-w-[1200px] flex-col items-start justify-between gap-6 px-5 py-16 sm:px-8 md:flex-row md:items-center"><div><h2 className="text-2xl font-semibold">Ready to run dinner service?</h2><p className="mt-2 text-sm text-muted-foreground">Pick the menu. Pick the crisis. Let the kitchen work.</p></div><Button asChild size="lg"><Link href="/kitchen">Start dinner rush <ArrowRight /></Link></Button></div></section></main>;
}
